#!/usr/bin/env ruby
require 'rubygems'
require 'smusher'

path = ARGV[0]
if ARGV.length != 1 or not File.exist?(path)
  puts "smusher DIRECTORY_OR_FILE_TO_OPTIMIZE"
  puts "smusher /apps/x/public/images"
  exit
end

if File.directory?(path)
  Smusher.store_smushed_folder(path)
else
  Smusher.store_smushed_image(path)
end
